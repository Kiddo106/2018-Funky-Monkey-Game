/*
inherits the rectangle class to determine if it intersects with an enemy or not.
declare an image variable

main_Character contructor method
 super(x,y,w,w); //This utilizes the Rectangle class and associates
this template for the current Frog object so
 this.img = img;
 
 
 show method
 image(img,x,y,w,w);
 
 move method
 changes the pixel location of the main_Character
 if the main character touches edges then
 do no move further.
*/
